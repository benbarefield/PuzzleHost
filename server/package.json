{
    "name": "puzzle-host-server",
    "watch": {
        "build": {
            "patterns": [
                "src"
            ],
            "extensions": "ts"
        }
    },
    "scripts": {
        "prestart": "npm run build",
        "start": "npm run watch & nodemon dist/index.js",
        "build": "npx tsc",
        "watch": "npm-watch"
    },
    "dependencies": {
        "express": "^4.19.2"
    },
    "devDependencies": {
        "@types/node": "^20.14.2",
        "nodemon": "^3.1.3",
        "npm-watch": "^0.13.0",
        "typescript": "^5.4.5"
    }
}
